export default function Home() {
  return (
    <main style={{ maxWidth: 720, margin: "0 auto", padding: "4rem 1.5rem" }}>
      <p style={{ textTransform: "uppercase", letterSpacing: "0.2em", fontSize: 12 }}>CustodIA</p>
      <h1 style={{ fontSize: 40, margin: "0.5rem 0" }}>Universal Agentic Finance Runtime</h1>
      <p style={{ color: "#555", lineHeight: 1.6 }}>
        The agent proposes, the human authorizes the boundary, the policy engine enforces it.
        Scaffold booting — the chat UI lands in step 7 of the build order.
      </p>
    </main>
  );
}
